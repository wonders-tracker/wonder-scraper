FROM mcr.microsoft.com/playwright:v1.49.0-jammy

WORKDIR /app

# Install playwright server
RUN npm init -y && npm install playwright@1.49.0

# Expose the browser server port
EXPOSE 3000

# Run playwright browser server
CMD ["npx", "playwright", "run-server", "--port", "3000", "--host", "0.0.0.0"]
